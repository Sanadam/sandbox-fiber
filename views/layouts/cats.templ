package layouts

templ Cats(cats map[string]string){
    <div class="cats">
        <ul>
            for k, v := range cats {
            <li class={ k }><a href="" class="cat-link">#{ v }</a></li>
            }
        </ul>
    </div>
}